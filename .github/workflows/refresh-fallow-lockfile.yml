name: Refresh Fallow lockfile

on:
  push:
    branches:
      - fix/opencode-reference-adoption

permissions:
  contents: write

jobs:
  refresh-lockfile:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Refresh only the Fallow resolution
        run: |
          cp pnpm-workspace.yaml /tmp/pnpm-workspace.yaml
          pnpm config set --location=project minimumReleaseAge 0
          pnpm install --lockfile-only --no-frozen-lockfile
          mv /tmp/pnpm-workspace.yaml pnpm-workspace.yaml
          git diff --exit-code -- package.json pnpm-workspace.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify Fallow CLI
        run: pnpm exec fallow --version

      - name: Verify dependency audit
        run: pnpm audit --audit-level=high

      - name: Commit targeted lockfile update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/refresh-fallow-lockfile.yml
          git add pnpm-lock.yaml
          git commit -m "chore(deps): refresh Fallow lockfile"
          git push origin HEAD:${{ github.ref_name }}
